import React, { FC } from 'react';

export const Forecast: FC = () => {
  return (
    <div className="w-full flex flex-col">
      <div>
        <h1 className="text-4xl">Weather <span className="font-semibold">Forecast</span></h1>
      </div>
    </div>
  )
}