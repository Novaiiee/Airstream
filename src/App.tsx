import React, { FC } from "react";
import { MainWrapper } from "./components/MainWrapper";

export const App: FC = () => {
	return (
		<div className="font-poppins">
			<MainWrapper />
		</div>
	);
};
